# 读伴 ReaDouble — Rokid AIUI 读论文陪伴智能体（工作区说明）

> 给 Claude 的说明文件。本文件夹是 `cnYui/readouble` 仓库的根目录；AIUI Studio 的导入根是其中的 `agent/` 子目录。

## 目录关系

| 位置 | 是什么 |
|---|---|
| 本文件夹 | Git 仓库根，GitHub：`https://github.com/cnYui/readouble`（公开，`main`） |
| `agent/` | **AIUI Studio 导入根**（直接含 `app.json`），AIUI 0.17.0，一个 Page：`pages/explain/index` 解读页 |
| `agent/lib/` | 纯逻辑：`reply.js`（输入归一化、提示词、三段回答解析、口令识别、Base64 / data URL、通知裁剪）、`temple.js`（镜腿输入去重） |
| `tests/` | Node 测试（`npm test`，Node 20+），`page.test.js` 直接加载 `.ink` 的 `<script setup>` 跑状态机；不进 Studio |
| `tools/build_audit.py` | 从 Skill 的能力清单生成 `docs/aiui-audit.md`（每次改完 `agent/` 都要重新生成，否则 `Project revision` 过期） |
| `C:\Users\yui\.claude\skills\rokid-aiui-agent` | 已安装的 Skill，校验脚本在 `scripts/` |

## 改代码 → Studio 调试的循环

1. 本地改 `agent/`，跑 `npm test`、`npm run validate`、`python tools/build_audit.py`。
2. `git commit` + `git push origin main`。
3. Studio（`https://aiui.rokid.com`）左上「新建智能体」整个按钮打开下拉 →「GitHub 导入」→ 地址框会保留上次的 `https://github.com/cnYui/readouble/tree/main/agent` →「确认导入」。同一地址再导入会就地更新，效果预览里的页面会重新加载。
4. 项目「···」（行尾三个点，hover 才明显）→「上传云端」→ 状态从「待上传」变「已同步」。
5. 对话框发 `/debug 模拟眼镜设备运行读伴页面 pages/explain/index`（先输入 `/debug` 等它变成标签，再点标签右侧输入文字，点发送按钮）。**不要写“解读页面”**——助手会把它理解成“解读页面代码”而拒绝。卡片上 hover 出现「进入」→ 画布进右上「效果预览」（480 × 352）。
6. 右侧「真机模拟」：镜腿四个按钮、语音输入（页面在聆听时可输入文字模拟识别，回车给中间结果，「发送」给最终结果）、摄像头（走浏览器 Webcam，弹「Webcam 拍照确认」）；「日志」面板显示页面的 `console.log('[readouble] …')`。
7. 左下角折叠侧栏后效果预览才能完整显示；浏览器面板收起时页面完全停摆。

## 模拟器实测（Studio 1.1.0，2026-09-11）

- 生命周期：`onTargetChanged(undefined → _current)` → `onLoad` → `onShow`；**没有 `onReady`**。自动拍照因此挂在首次 `onShow`（`_autoAttempted` 保证只一次）。
- 单击 → `GlobalHook`(down/up) + `Enter`(down/up)；向前滑动 → `GlobalHook` + `ArrowUp`；向后滑动 → `GlobalHook` + `ArrowDown`；双击不送到页面。`lib/temple.js` 让紧跟手势键的 `GlobalHook` 失效。
- `wx.media.createCameraContext().takePhoto()` 在 Studio 里弹 Webcam 对话框；浏览器拒绝摄像头时只抛 `failed Exception generated by QuickJS`（没有 permission 字样），所以连续两次失败后改走“念出这段文字”的听写路径。Claude 的内置浏览器不允许摄像头，真实拍照要在用户自己的浏览器或真机上验证。
- `SpeechRecognition`：Studio 不触发 `onstart`（空闲计时器要在 `start()` 后自己启动）；中间结果 `isFinal=false`，「发送」后 `isFinal=true` 并 `onend`。用内置浏览器 `type` 中文没有输入进去，英文可以。
- `LanguageModel.promptStreaming()` 首轮约 30 s，第二轮曾超过 45 s，所以看门狗改为“每收到一个 chunk 重置，75 s 无进展才超时”。回答确实按【原文】【解读】【术语】三段返回。
- `speechSynthesis`：右侧出现「TTS」卡片显示朗读文本。
- `text-overflow: ellipsis` 在 Ink 里不裁剪长文本，通知文字要在 JS 里裁到 18 字。
- `<page>` 根节点动态类不生效；`ink:for` 用 `<block ink:for … ink:for-item="x">`；被 `ink:if` 销毁重建的块里 `ink:for` 不再渲染，所以四个面板常驻、用 `on` 类显示。
- 草稿态智能体的槽位抽取在模拟器里不生效（`/debug` 打开的页面 `query={}`），页面必须能在没有 `question` 时工作。

## 页面状态机（`_phase`）

`ready` → 单击拍照 → `capturing` → `reading`（`session.prompt` 图片+问题）→ `answered` → 单击 → `listening` → 口令或追问 → `reading`（`promptStreaming`）→ `answered`。相机被拒 / 不可用 / 连续失败 → `error(camera)` → 单击 → 听写；没有照片时第一段听写是原文，有了回答之后的听写都是追问。任何异步结果都带 turn id，`onHide` 会把 turn 作废并停止听写、朗读、相机。

## 已验证 / 未验证

见 README「Studio 模拟器里已验证」「未验证」和 `docs/aiui-audit.md`。模拟器结论 ≠ 真机通过。
