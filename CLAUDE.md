# 读伴 ReaDouble — Rokid AIUI 读论文陪伴智能体（工作区说明）

> 给 Claude 的说明文件。本文件夹是 `cnYui/readouble` 仓库的根目录；AIUI Studio 的导入根是其中的 `agent/` 子目录。

## 目录关系

| 位置 | 是什么 |
|---|---|
| 本文件夹 | Git 仓库根，GitHub：`https://github.com/cnYui/readouble`（**公开**，`main`） |
| `agent/` | **AIUI Studio 导入根**（直接含 `app.json`），AIUI 0.17.0，一个 Page：`pages/explain/index` 解读页 |
| `agent/config/vision.js` | 视觉中转站配置；`apiKey` 在 Git 中必须为空 |
| `agent/lib/` | `reply.js`（提示词、三段解析、`NO_IMAGE`、口令、Base64）、`vision.js`（中转站请求 / 错误 / 历史裁剪）、`temple.js`（镜腿去重） |
| `tests/` | Node 测试（`npm test`，Node 20+）；`page.test.js` 宿主路径，`page.relay.test.js` 中转站路径（`tests/fixtures/vision-config.js` 假密钥），`relay.integration.test.js` 真实中转站（需要环境变量），`secrets.test.js` 防止密钥进 `agent/` |
| `tools/build_audit.py` | 从 Skill 的能力清单生成 `docs/aiui-audit.md`；每次改完 `agent/` 都要重新生成 |
| `C:\Users\yui\.claude\skills\rokid-aiui-agent` | 已安装的 Skill，校验脚本在 `scripts/` |

## 密钥

- 密钥只放在仓库根目录的 `.env`（在 `.gitignore` 里），模板是 `.env.example`。绝不写进任何被 Git 跟踪的文件，也不要在 Studio 的网页编辑器里替用户填密钥。
- `npm run build:agent` 生成 `build/agent/`（也被忽略），其中 `config/vision.js` 带着 `.env` 的配置；用户在 Studio 对项目「···」▸「本地导入」选这个目录再「上传云端」。
- `npm run test:live` 用 `.env` 跑真实中转站测试；`npm test` 保持离线，并用 `tests/secrets.test.js` 检查被跟踪的文件里没有密钥、`.env` 和 `build/` 仍被忽略。

## 改代码 → Studio 调试的循环

1. 本地改 `agent/`，跑 `npm test`、`npm run validate`、`python tools/build_audit.py`（再用 Skill 的 `validate_aiui_audit.py` 校验，预期退出码 2 = 有效但 BLOCKED）。
2. `git commit` + `git push origin main`。
3. Studio（`https://aiui.rokid.com`）左上「新建智能体」整个按钮打开下拉 →「GitHub 导入」→ 地址框会保留上次的地址 →「确认导入」。
4. 项目「···」（行尾三个点）→「上传云端」→ 状态变「已同步」。
5. 对话框发 `/debug 模拟眼镜设备运行读伴页面 pages/explain/index`（先输入 `/debug` 等它变成标签，再输入文字，点发送）。不要写“解读页面”——助手会当成“解读页面代码”而拒绝。卡片上 hover 出现「进入」。
6. 日志第一行 `explain onLoad … build=<BUILD> vision=host|relay:<model>` 说明 Studio 实际跑的是哪个版本、用哪个模型。
7. 左下角折叠侧栏后效果预览才能完整显示；浏览器面板收起时页面完全停摆。

## Studio 1.1.0 实测（2026-09-11）

- 生命周期：`onTargetChanged(undefined → _current)` → `onLoad` → `onShow`；没有 `onReady`。
- 单击 → `GlobalHook` + `Enter`；滑动 → `GlobalHook` + `ArrowUp/ArrowDown`；双击不送到页面。
- 「摄像头」面板上传的本地图片按原字节交给 `takePhoto()`；用浏览器摄像头时 Claude 的内置浏览器会拒绝，只抛 `failed Exception generated by QuickJS`。
- 宿主 `LanguageModel` → `POST https://rcs.rokid.com/metis/api/chat/completions`（OpenAI 兼容、SSE）。草稿态默认模型**不读图**：照片确实在 342,764 字节的请求体里，回答却是编造的 Transformer 段落。「网络」面板不采集流式响应正文，请求头里有用户的 access_token——不要复制它。
- `SpeechRecognition`：不触发 `onstart`；内置浏览器里输入中文进不去「语音输入」框，英文可以。
- `text-overflow: ellipsis` 不生效；`<page>` 根节点动态类不生效；`ink:for` 用 `<block ink:for … ink:for-item="x">`；被 `ink:if` 销毁重建的块里 `ink:for` 不再渲染。

## 中转站实测（2026-09-11）

- 当前默认 `https://api2.ai-genesis.app/v1`：新密钥的 `/v1/models` 有 7 个模型，其中 `gpt-5.5`、`gpt-5.6-sol`、`gpt-5.6-terra`。`gpt-5.5` + `reasoning_effort: medium` 读图正确（引用 `LRW-7391` 和图中 “38%”），追问、翻译都继续看图；三轮 40 / 21 / 64 秒，`test:live` 一次 71 秒。
- 跨域：预检 204、`Access-Control-Allow-Origin: *`、允许 `Authorization`；在 `https://aiui.rokid.com` 页面里直接 `fetch` 能读到 401 JSON → Studio 网页模拟器可以请求它（前提是导入带密钥的 `build/agent`）。
- 旧的 `https://api.aaccx.pw/v1`：`gpt-5.5` 读图正确但每轮 27–96 秒；预检 403，浏览器里 `Failed to fetch`；`grok-4.5` 编造图片内容。
- 超时：`config/vision.js` 的 `timeoutMs` 是 150000，页面整轮看门狗是它加 10 秒（`_turnTimeoutMs()`）。
- `agent/` 里没有任何模拟数据。没有密钥时页面退回宿主 `LanguageModel`（Studio 草稿态不读图，会编内容）；有密钥时只走中转站，失败直接报错（`tests/page.relay.test.js` 锁住了这一点）。
- 换模型、换中转站或换密钥后，先跑 `npm run test:live`（读 `.env`），回答里必须引用测试图里的 `LRW-7391`。

## 页面状态机（`_phase`）

`ready` → 单击拍照 → `capturing` → `reading`（中转站或宿主，图片+问题）→ `answered` → 单击 → `listening` → 口令或追问 → `reading` → `answered`。模型回 `NO_IMAGE`、相机被拒 / 不可用 / 连续失败 → `error` → 单击 → 听写。所有异步结果都带 turn id；`onHide` 作废 turn，停止听写、朗读、相机，并 abort 中转站请求。
